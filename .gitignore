# ---- Python (CDK infra, Lambda app, eval) ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
env/

# ---- Environment / secrets ----
.env
.env.*
*.local

# ---- AWS CDK ----
cdk.out/
.cdk.staging/
cdk.context.json

# ---- Node (only for the widget's tooling, if any) ----
frontend/node_modules/
node_modules/

# ---- IDE / OS ----
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- Claude ---
.claude

# --- Scraper ---
scraper_output/

# --- Eval ---
# promptfoo run output: a ~1MB per-run dump of live answers and judge reasoning. Useful while
# tuning, never a repo artifact - it is a snapshot of one deploy at one moment.
eval/promptfoo/results.json