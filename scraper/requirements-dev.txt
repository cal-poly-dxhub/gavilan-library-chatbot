# Dev/test deps for the scraper. Runtime deps (httpx, trafilatura, PyYAML) are in
# requirements.txt; the tests need those too, so install both:
#
#     python -m pip install -r requirements.txt -r requirements-dev.txt
#
# Pinned to the same pytest as infra/ and eval/ so all three suites behave identically.
pytest==8.4.2
